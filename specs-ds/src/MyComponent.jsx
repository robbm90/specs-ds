export default function MyComponent() {
  return (
    <div
      style={{
        padding: "24px",
        backgroundColor: "#111",
        color: "#eaff00",
        borderRadius: "8px",
        fontFamily: "sans-serif",
      }}
    >
      <h3>🚀 React Component</h3>
      <p>Este componente foi renderizado dentro do HTML do seu MVP.</p>
    </div>
  );
}
